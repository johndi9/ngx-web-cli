import { MonthsDurationPipe } from './months-duration.pipe';

describe('MonthsDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new MonthsDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
